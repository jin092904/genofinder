[project]
name = "genofinder-shared-schemas"
version = "0.1.0"
description = "Shared Pydantic models — codegen target for zod (web)"
requires-python = ">=3.12,<3.13"
license = { text = "Proprietary" }

dependencies = [
    # TODO(verify): pydantic v2 PyPI 검증 후 추가
    # "pydantic>=2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
